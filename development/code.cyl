include#<System>;
include#<Chrono>;

const start = Chrono.Now();

var i = 0;
while (i < 1000000) {
  i++;
}

//Chrono.Sleep(1000);

const elapsed = Chrono.Now() - start;
System.PrintT("Elapsed: %ims", elapsed);

