include#<SystemLogging>;
include#<Chrono>;

var j = System.Prompt("How many? (1000000) ", "Number");
if (j === null) {
	j = 1000000;
}

const start = Time.Now();

var i = 0;
while (i < j) {
	i++;
}

System.Log("Elapsed: " + (Time.Now() - start) + "ms");